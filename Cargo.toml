[package]
name = "loomex-protocol"
version = "0.1.0"
edition = "2021"
license = "UNLICENSED"
repository = "https://github.com/loomex-app/loomex-protocol"
description = "Versioned Loomex runner contracts shared by desktop and plugin runtimes"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
